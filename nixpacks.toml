[phases.setup]
nixPkgs = ["php82", "php82Packages.composer"]

[phases.install]
cmds = ["composer install --ignore-platform-reqs --optimize-autoloader --no-dev --no-scripts --no-interaction"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
